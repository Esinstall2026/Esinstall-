import { ChangeEvent, type ReactNode, useMemo, useState } from "react";
import { builders, communities, jobs as seedJobs, teams } from "./data";
import { Job, JobStatus, Page, ProductionRecord, ProductionStatus } from "./types";
import { loadProduction, saveProduction } from "./production";
const nav: { label: Page; icon: string }[] = [{label:"Dashboard",icon:"▦"},{label:"Jobs",icon:"▤"},{label:"Job Folder",icon:"▱"},{label:"Production",icon:"▰"},{label:"Installations",icon:"⌂"},{label:"Warranty",icon:"◇"},{label:"Reports",icon:"▥"},{label:"Teams",icon:"♙"},{label:"Builders",icon:"▣"},{label:"Communities",icon:"⌘"},{label:"Settings",icon:"⚙"}];
const statuses: JobStatus[]=["Scheduled","Production","Ready for Installation","Installation","Completed","Warranty"];
type Installation={jobId:string;team:string;date:string;time:string;status:"Scheduled"|"In Progress"|"Completed";notes:string};
type WarrantyCase={jobId:string;openedAt:string;issue:string;status:"Open"|"In Review"|"Resolved";priority:"Low"|"Medium"|"High";notes:string};
const installSeed:Installation[]=seedJobs.map((j,i)=>({jobId:j.id,team:j.team,date:j.date,time:i%2?"10:00 AM":"8:00 AM",status:j.status==="Installation"?"In Progress":"Scheduled",notes:""}));
function App(){const[page,setPage]=useState<Page>("Dashboard");const[jobList,setJobList]=useState<Job[]>(()=>loadJobs());const[selectedJobId,setSelectedJobId]=useState(jobList[0]?.id??"");const[search,setSearch]=useState("");const[installations,setInstallations]=useState<Installation[]>(()=>loadInstalls());const[warranties,setWarranties]=useState<WarrantyCase[]>(()=>loadWarranties());const[production,setProduction]=useState<ProductionRecord[]>(()=>loadProduction());const selectedJob=jobList.find(j=>j.id===selectedJobId)??null;const filteredJobs=useMemo(()=>jobList.filter(j=>`${j.id} ${j.address} ${j.builder} ${j.community}`.toLowerCase().includes(search.toLowerCase())),[jobList,search]);const openJob=(job:Job)=>{setSelectedJobId(job.id);setPage("Job Folder")};const updateJob=(updated:Job)=>{const next=jobList.map(j=>j.id===updated.id?updated:j);setJobList(next);saveJobs(next)};const updateInstall=(value:Installation)=>{const next=installations.some(i=>i.jobId===value.jobId)?installations.map(i=>i.jobId===value.jobId?value:i):[...installations,value];setInstallations(next);saveInstalls(next)};const updateWarranty=(value:WarrantyCase)=>{const next=warranties.some(w=>w.jobId===value.jobId)?warranties.map(w=>w.jobId===value.jobId?value:w):[...warranties,value];setWarranties(next);saveWarranties(next)};const updateProduction=(value:ProductionRecord)=>{const next=production.some(p=>p.jobId===value.jobId)?production.map(p=>p.jobId===value.jobId?value:p):[...production,value];setProduction(next);saveProduction(next);const job=jobList.find(j=>j.id===value.jobId);if(job){const mapped:JobStatus=value.status==="Ready"?"Ready for Installation":value.status==="Pending"?"Scheduled":"Production";updateJob({...job,status:mapped});}};return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">ES</div><div><strong>ES INSTALL</strong><span>Operations Platform</span></div></div><div className="nav-title">MAIN MENU</div><nav>{nav.map(item=><button key={item.label} className={`nav-item ${page===item.label?"active":""}`} onClick={()=>setPage(item.label)}><span className="nav-icon">{item.icon}</span>{item.label}</button>)}</nav><div className="sidebar-footer"><div className="status-dot"/><div><strong>System Ready</strong><span>ES INSTALL v0.4</span></div></div></aside><main className="main"><header className="topbar"><div><div className="eyebrow">ES INSTALL / {page.toUpperCase()}</div><h1>{page}</h1></div><div className="user-chip"><div className="avatar">AD</div><div><strong>Administrator</strong><span>Admin</span></div></div></header>{page==="Dashboard"&&<Dashboard jobs={jobList} onOpenJob={openJob}/>} {page==="Jobs"&&<JobsPage jobs={filteredJobs} search={search} setSearch={setSearch} onOpenJob={openJob}/>} {page==="Job Folder"&&selectedJob&&<JobFolder job={selectedJob} onSave={updateJob}/>} {page==="Production"&&<ProductionPage jobs={jobList} records={production} onSave={updateProduction} onOpenJob={openJob}/>} {page==="Installations"&&<InstallationsPage jobs={jobList} installations={installations} onSave={updateInstall} onOpenJob={openJob}/>} {page==="Warranty"&&<WarrantyPage jobs={jobList} installations={installations} warranties={warranties} onSave={updateWarranty} onOpenJob={openJob}/>} {page!=="Dashboard"&&page!=="Jobs"&&page!=="Job Folder"&&page!=="Production"&&page!=="Installations"&&page!=="Warranty"&&<ModulePlaceholder page={page}/>}</main></div>}
function ProductionPage({jobs,records,onSave,onOpenJob}:{jobs:Job[];records:ProductionRecord[];onSave:(p:ProductionRecord)=>void;onOpenJob:(j:Job)=>void}){const[filter,setFilter]=useState("");const rows=records.filter(p=>{const j=jobs.find(x=>x.id===p.jobId);return j&&`${p.jobId} ${j.address} ${j.builder} ${j.community} ${p.material} ${p.status}`.toLowerCase().includes(filter.toLowerCase())});const counts=(s:ProductionStatus)=>records.filter(p=>p.status===s).length;return <div className="content"><section className="page-tools"><div><span className="gold-label">FACTORY OPERATIONS</span><h2>Production / Factory</h2><p className="muted">Track slabs, cutting, polishing, separation and readiness for installation.</p></div><div className="button-group"><button className="ghost" onClick={()=>setFilter("")}>All Jobs</button></div></section><div className="metric-grid"><div className="metric-card"><span>Pending</span><div className="metric-value">{counts("Pending")}</div><small>awaiting production</small></div><div className="metric-card"><span>Cutting</span><div className="metric-value">{counts("Cutting")}</div><small>in fabrication</small></div><div className="metric-card"><span>Polishing</span><div className="metric-value">{counts("Polishing")}</div><small>finishing</small></div><div className="metric-card"><span>Ready</span><div className="metric-value">{counts("Ready")}</div><small>for installation</small></div></div><div className="search-box"><span>⌕</span><input value={filter} onChange={e=>setFilter(e.target.value)} placeholder="Search job, address, builder, material or status..."/></div><section className="panel table-panel"><div className="table-head"><span>JOB</span><span>MATERIAL / SLABS</span><span>SQ FT</span><span>SINK / MATERIALS</span><span>STATUS</span><span/></div>{rows.map(p=>{const j=jobs.find(x=>x.id===p.jobId)!;return <div className="table-row" key={p.jobId}><button style={plainButton} onClick={()=>onOpenJob(j)}>{p.jobId}</button><span><select value={p.material} onChange={e=>onSave({...p,material:e.target.value as ProductionRecord["material"]})} style={selectStyle}>{["Granite","Marble","Porcelain"].map(v=><option key={v}>{v}</option>)}</select><input type="number" min={0} value={p.slabCount} onChange={e=>onSave({...p,slabCount:Number(e.target.value)})} style={inputStyle}/></span><input type="number" min={0} value={p.squareFeet} onChange={e=>onSave({...p,squareFeet:Number(e.target.value)})} style={inputStyle}/><span><select value={p.sinkType} onChange={e=>onSave({...p,sinkType:e.target.value as ProductionRecord["sinkType"]})} style={selectStyle}>{["Undermount","Farmhouse","Topmount","None"].map(v=><option key={v}>{v}</option>)}</select><small>{p.caulkTubes} caulk · {p.clips} clips</small></span><select value={p.status} onChange={e=>onSave({...p,status:e.target.value as ProductionStatus,updatedAt:new Date().toISOString()})} style={selectStyle}>{["Pending","Cutting","Polishing","Separated","Ready"].map(v=><option key={v}>{v}</option>)}</select><button className="ghost" onClick={()=>onOpenJob(j)}>Job →</button></div>)}</section></div>}
function Dashboard({jobs,onOpenJob}:{jobs:Job[];onOpenJob:(j:Job)=>void}){return <div className="content"><section className="hero"><div><span className="gold-label">OPERATIONS OVERVIEW</span><h2>Good morning, Administrator.</h2><p>Monitor jobs, production, installations and warranties from one place.</p></div><button className="primary" onClick={()=>jobs[0]&&onOpenJob(jobs[0])}>Open latest Job</button></section><section className="metric-grid">{[["Jobs in Progress","18","+12%"],["Installations Today","7","+2"],["Open Warranties","4","-18%"],["Teams in Field","6","100%"]].map(([a,b,c])=><div className="metric-card" key={a}><span>{a}</span><div className="metric-value">{b}</div><small>{c} this week</small></div>)}</section><div className="two-column"><section className="panel"><div className="panel-head"><div><span className="gold-label">OPERATIONS</span><h3>Active Jobs</h3></div></div><div className="job-list">{jobs.slice(0,4).map(j=><button className="job-row" key={j.id} onClick={()=>onOpenJob(j)}><div className="job-code">{j.id}</div><div className="job-main"><strong>{j.address}</strong><span>{j.builder} · {j.community}</span></div><Status status={j.status}/><span className="arrow">→</span></button>)}</div></section><section className="panel"><div className="panel-head"><div><span className="gold-label">WEEK</span><h3>Installation Agenda</h3></div></div><div className="agenda">{["MON","TUE","WED","THU","FRI"].map((d,i)=><div className="agenda-day" key={d}><span>{d}</span><strong>{[3,4,2,5,3][i]}</strong><small>installs</small></div>)}</div></section></div></div>}
function JobsPage({jobs,search,setSearch,onOpenJob}:{jobs:Job[];search:string;setSearch:(v:string)=>void;onOpenJob:(j:Job)=>void}){return <div className="content"><section className="page-tools"><div><span className="gold-label">JOB MANAGEMENT</span><h2>All Jobs</h2></div><button className="primary">+ New Job</button></section><div className="search-box"><span>⌕</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search by job, address, builder or community..."/></div><section className="panel table-panel"><div className="table-head"><span>JOB</span><span>LOCATION</span><span>BUILDER</span><span>TEAM</span><span>STATUS</span><span/></div>{jobs.map(j=><button className="table-row" key={j.id} onClick={()=>onOpenJob(j)}><strong>{j.id}</strong><span>{j.address}<small>{j.community}</small></span><span>{j.builder}</span><span>{j.team}</span><Status status={j.status}/><span>→</span></button>)}</section></div>}
function InstallationsPage({jobs,installations,onSave,onOpenJob}:{jobs:Job[];installations:Installation[];onSave:(i:Installation)=>void;onOpenJob:(j:Job)=>void}){const[filter,setFilter]=useState("");const rows=installations.filter(i=>{const j=jobs.find(x=>x.id===i.jobId);return j&&`${i.jobId} ${j.address} ${j.builder} ${j.community} ${i.team} ${i.date}`.toLowerCase().includes(filter.toLowerCase())});const today=new Date().toISOString().slice(0,10);return <div className="content"><section className="page-tools"><div><span className="gold-label">FIELD OPERATIONS</span><h2>Installations</h2><p className="muted">Schedule jobs, assign teams and track completion.</p></div><div className="button-group"><button className="ghost" onClick={()=>setFilter(today)}>Today</button><button className="primary" onClick={()=>setFilter("")}>All Jobs</button></div></section><div className="metric-grid"><div className="metric-card"><span>Scheduled</span><div className="metric-value">{rows.filter(i=>i.status==="Scheduled").length}</div><small>ready for field</small></div><div className="metric-card"><span>In Progress</span><div className="metric-value">{rows.filter(i=>i.status==="In Progress").length}</div><small>teams working</small></div><div className="metric-card"><span>Completed</span><div className="metric-value">{rows.filter(i=>i.status==="Completed").length}</div><small>closed installs</small></div><div className="metric-card"><span>Teams</span><div className="metric-value">{new Set(rows.map(i=>i.team)).size}</div><small>assigned</small></div></div><div className="search-box"><span>⌕</span><input value={filter} onChange={e=>setFilter(e.target.value)} placeholder="Search job, address, builder, community or team..."/></div><section className="panel table-panel"><div className="table-head"><span>JOB</span><span>LOCATION</span><span>TEAM</span><span>DATE / TIME</span><span>STATUS</span><span/></div>{rows.map(i=>{const j=jobs.find(x=>x.id===i.jobId)!;return <div className="table-row" key={i.jobId}><button style={plainButton} onClick={()=>onOpenJob(j)}>{i.jobId}</button><span>{j.address}<small>{j.builder} · {j.community}</small></span><select value={i.team} onChange={e=>onSave({...i,team:e.target.value})} style={selectStyle}>{teams.map(t=><option key={t}>{t}</option>)}</select><span><input type="date" value={i.date} onChange={e=>onSave({...i,date:e.target.value})} style={inputStyle}/><select value={i.time} onChange={e=>onSave({...i,time:e.target.value})} style={inputStyle}>{["8:00 AM","10:00 AM","12:00 PM","2:00 PM","4:00 PM"].map(t=><option key={t}>{t}</option>)}</select></span><select value={i.status} onChange={e=>onSave({...i,status:e.target.value as Installation["status"]})} style={selectStyle}><option>Scheduled</option><option>In Progress</option><option>Completed</option></select><button className="ghost" onClick={()=>onOpenJob(j)}>Job →</button></div>})}</section></div>}
function WarrantyPage({jobs,installations,warranties,onSave,onOpenJob}:{jobs:Job[];installations:Installation[];warranties:WarrantyCase[];onSave:(w:WarrantyCase)=>void;onOpenJob:(j:Job)=>void}){const[filter,setFilter]=useState("");const eligible=jobs.filter(j=>{const i=installations.find(x=>x.jobId===j.id);return i?.status==="Completed"||j.status==="Warranty"||warranties.some(w=>w.jobId===j.id)});const rows=eligible.filter(j=>`${j.id} ${j.address} ${j.builder} ${j.community}`.toLowerCase().includes(filter.toLowerCase()));return <div className="content"><section className="page-tools"><div><span className="gold-label">AFTER-SALES</span><h2>Warranty</h2><p className="muted">Track post-installation issues, evidence and resolution.</p></div><button className="primary" onClick={()=>rows[0]&&onSave({jobId:rows[0].id,openedAt:new Date().toISOString(),issue:"New warranty request",status:"Open",priority:"Medium",notes:""})}>+ New Warranty</button></section><div className="metric-grid"><div className="metric-card"><span>Open</span><div className="metric-value">{warranties.filter(w=>w.status==="Open").length}</div><small>needs action</small></div><div className="metric-card"><span>In Review</span><div className="metric-value">{warranties.filter(w=>w.status==="In Review").length}</div><small>being assessed</small></div><div className="metric-card"><span>Resolved</span><div className="metric-value">{warranties.filter(w=>w.status==="Resolved").length}</div><small>closed cases</small></div><div className="metric-card"><span>Eligible Jobs</span><div className="metric-value">{eligible.length}</div><small>completed installations</small></div></div><div className="search-box"><span>⌕</span><input value={filter} onChange={e=>setFilter(e.target.value)} placeholder="Search warranty by job, address, builder or community..."/></div><section className="panel table-panel"><div className="table-head"><span>JOB</span><span>LOCATION</span><span>CASE</span><span>PRIORITY</span><span>STATUS</span><span/></div>{rows.map(j=>{const w=warranties.find(x=>x.jobId===j.id);return <div className="table-row" key={j.id}><button style={plainButton} onClick={()=>onOpenJob(j)}>{j.id}</button><span>{j.address}<small>{j.builder} · {j.community}</small></span>{w?<input value={w.issue} onChange={e=>onSave({...w,issue:e.target.value})} style={inputStyle}/>:<button className="ghost" onClick={()=>onSave({jobId:j.id,openedAt:new Date().toISOString(),issue:"Installation issue",status:"Open",priority:"Medium",notes:""})}>Open Case</button>}{w?<select value={w.priority} onChange={e=>onSave({...w,priority:e.target.value as WarrantyCase["priority"]})} style={selectStyle}><option>Low</option><option>Medium</option><option>High</option></select>:<span className="muted">—</span>}{w?<select value={w.status} onChange={e=>onSave({...w,status:e.target.value as WarrantyCase["status"]})} style={selectStyle}><option>Open</option><option>In Review</option><option>Resolved</option></select>:<span className="muted">Not opened</span>}<button className="ghost" onClick={()=>onOpenJob(j)}>Job →</button></div>})}</section>{warranties.length>0&&<section className="panel" style={{marginTop:14}}><div className="panel-head"><div><span className="gold-label">CASE DETAILS</span><h3>Warranty Notes</h3></div></div>{warranties.map(w=><div className="history-row" key={w.jobId}><span>{w.jobId}</span><div><strong>{w.issue}</strong><small>{w.status} · {w.priority} · opened {new Date(w.openedAt).toLocaleDateString()}</small><textarea value={w.notes} onChange={e=>onSave({...w,notes:e.target.value})} placeholder="Add resolution notes..." /></div></div>)}</section>}</div>}
const plainButton={background:"none",border:0,color:"inherit",textAlign:"left",fontWeight:800,cursor:"pointer",padding:0};const selectStyle={background:"#0c0c0e",color:"#fff",border:"1px solid #27272a",borderRadius:6,padding:7,fontSize:10,width:"100%"};const inputStyle={background:"#0c0c0e",color:"#fff",border:"1px solid #27272a",borderRadius:6,padding:7,fontSize:10,width:"100%",marginBottom:4};
function JobFolder({job,onSave}:{job:Job;onSave:(j:Job)=>void}){const[editing,setEditing]=useState(false);const[draft,setDraft]=useState(job);const[evidence,setEvidence]=useState<Evidence[]>(()=>loadEvidence(job.id));const add=(e:ChangeEvent<HTMLInputElement>)=>{const files=Array.from(e.target.files??[]);if(!files.length)return;const next=[...evidence,...files.map(f=>({name:f.name,type:f.type||"file",size:f.size,addedAt:new Date().toISOString()}))];setEvidence(next);saveEvidence(job.id,next);e.target.value=""};const remove=(name:string,at:string)=>{const next=evidence.filter(x=>!(x.name===name&&x.addedAt===at));setEvidence(next);saveEvidence(job.id,next)};const save=()=>{onSave(draft);setEditing(false)};return <div className="content"><section className="job-folder-head"><... (truncated)