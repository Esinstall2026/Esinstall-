# V8 Complete

The V8 workflow foundation is committed on `codex/v8-foundation`.

Completed in V8:
- shared workflow item model;
- job-to-workflow synchronization;
- team, priority and status transitions;
- workflow integrity validation;
- seeded-job acceptance smoke check.

V9 can now build on this layer without changing the V8 contract.
